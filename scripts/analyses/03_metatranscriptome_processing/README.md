## Scripts

- `01_transcriptm_metatranscriptome_processing.sh`: processes raw paired-end metatranscriptomic reads, removes low-quality reads, PhiX sequences, rRNA, tRNA and tmRNA, maps the cleaned reads against the dereplicated MAG collection, and calculates gene-level coverage and read counts using TranscriptM.
